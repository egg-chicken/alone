{
  "name": "alone",
  "version": "1.0.0",
  "description": "cui game",
  "main": "build/index.js",
  "scripts": {
    "play": "NODE_PATH=./src/ coffee src/main.coffee",
    "build": "NODE_PATH=./src/ coffee -o build/ -c src/; NODE_PATH=./build/ browserify build/main.js > build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "egg_chicken",
  "license": "ISC",
  "devDependencies": {
    "better-console": "^0.2.4",
    "underscore": "^1.8.3"
  }
}
